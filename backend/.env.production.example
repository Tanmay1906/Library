# Example production env for backend (DO NOT commit secrets)
# Copy to backend/.env.production and fill values.

DATABASE_URL=postgresql://<NEON_USER>:<NEON_PASSWORD>@<NEON_HOST>:<NEON_PORT>/<NEON_DB>?sslmode=require
JWT_SECRET=replace_with_secure_random
FRONTEND_URL=https://your-frontend.example
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@example.com
EMAIL_PASSWORD=your-email-app-password
BCRYPT_SALT_ROUNDS=12

# Other env vars from the original .env.production can be added as needed
