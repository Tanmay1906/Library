# Example Neon env file (DO NOT store secrets in source control)
# Copy to .env.neon and fill values before running migration script.

# Local Postgres (source)
LOCAL_PG_URL=postgresql://librarymate_user:librarymate_secure_password_2025@localhost:5433/librarymate

# Neon Postgres (target) - get this from Neon dashboard
NEON_PG_URL=postgresql://<NEON_USER>:<NEON_PASSWORD>@<NEON_HOST>:<NEON_PORT>/<NEON_DB>?sslmode=require

# Optional: if set to 1, local dump will be removed after migration
CLEANUP=1
